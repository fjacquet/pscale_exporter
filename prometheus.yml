global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/pscale.rules.yml

scrape_configs:
  - job_name: powerscale
    metrics_path: /metrics
    static_configs:
      - targets: ["pscale_exporter:2112"]
