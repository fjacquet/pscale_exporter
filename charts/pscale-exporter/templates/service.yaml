apiVersion: v1
kind: Service
metadata:
  name: {{ include "pscale-exporter.fullname" . }}
  labels:
    {{- include "pscale-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pscale-exporter.selectorLabels" . | nindent 4 }}
